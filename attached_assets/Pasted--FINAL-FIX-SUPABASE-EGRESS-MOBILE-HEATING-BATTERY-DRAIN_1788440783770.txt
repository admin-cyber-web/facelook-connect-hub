# FINAL FIX — SUPABASE EGRESS + MOBILE HEATING + BATTERY DRAIN

IMPORTANT: Stop doing audit-only work.

The objective of this task is to ACTUALLY FIX the application's excessive resource usage.

Original problem:

* One user can cause 1GB+ Supabase/network usage per day.
* Mobile phone heats up shortly after opening the app.
* Battery drains rapidly.
* App must remain fully functional.

The previous performance deep-clean has already been completed. PRESERVE those fixes.

## DO NOT START ANOTHER REDESIGN OR PROFILING-ONLY PASS.

This is now an IMPLEMENTATION/HARDENING task.

---

# 1. ROOT OBJECTIVE

Reduce unnecessary:

* Supabase egress
* Supabase database traffic
* Supabase Realtime traffic
* Supabase Storage downloads
* image downloads
* video downloads
* repeated API requests
* CPU usage
* GPU usage
* timers
* background media processing
* battery consumption

WITHOUT removing existing features.

---

# 2. IMPORTANT: DO NOT MISUNDERSTAND "CACHE EGRESS"

Do not assume the problem is browser cache.

Find and eliminate the reason data is being repeatedly downloaded.

Audit the complete application for:

```text
supabase.from(
supabase.rpc(
.channel(
.subscribe(
fetch(
getSession(
getUser(
setInterval(
setTimeout(
watchPosition(
createObjectURL(
```

Also search for:

```text
useEffect(
visibilitychange
focus
blur
online
offline
IntersectionObserver
```

Identify anything that causes repeated work.

---

# 3. SUPABASE DATABASE TRAFFIC

Every repeated Supabase query must have a legitimate reason.

Fix confirmed excessive queries caused by:

* React re-renders
* useEffect dependency mistakes
* navigation remounts
* duplicate components
* repeated auth listeners
* repeated profile loading
* repeated notification loading
* repeated feed loading
* repeated comments loading
* unnecessary refresh loops

Do NOT allow:

```text
one screen open
→ same query
→ same query
→ same query
→ same query
```

If data is already available and valid in application state, do not immediately download it again.

Use appropriate in-memory reuse/caching where safe.

Do not cache private data insecurely.

---

# 4. FEED

The Home/Fame Feed must NOT repeatedly download the entire feed.

Implement/verify:

* bounded initial query
* pagination
* incremental loading
* no automatic full-feed refresh loop
* no duplicate feed requests
* no full-feed refetch after a like
* no full-feed refetch after a comment
* no full-feed refetch after unrelated Realtime events

A single changed post should update only the necessary state whenever safely possible.

---

# 5. REALTIME

Do NOT disable Realtime globally.

Instead:

For every `.channel()`:

* ensure one subscription per required scope
* use proper filters
* prevent duplicate subscriptions
* remove channel when its owning screen/component is destroyed
* stop unnecessary subscriptions when the feature is inactive
* prevent subscriptions on every render

Navigation test must behave like:

```text
Home: 3 channels
Profile: required channels only
Back Home: approximately same stable channel count
Chat: Chat channels added
Leave Chat: Chat channels removed
```

It must NOT become:

```text
3
6
9
12
15
```

---

# 6. CHAT

Preserve realtime chat.

Do NOT replace realtime with polling.

If fallback polling exists:

* only run while Chat is active
* stop when Chat is hidden
* stop when leaving Chat
* use conservative interval
* never run duplicate polling loops

Receiving one message must NOT trigger:

```text
full Home reload
full Profile reload
full Chat reload
full Notification reload
```

Only update the required conversation/message state.

Historical messages must be paginated.

---

# 7. NOTIFICATIONS

Check notification loading carefully.

Prevent:

* repeated notification queries
* duplicate notification listeners
* notification query on every render
* notification query on every navigation
* full notification reload caused by unrelated events

Realtime notification events should update only the notification state.

---

# 8. PROFILE / USER DATA

Prevent repeated:

```text
getSession()
getUser()
profile query
```

caused by rendering/navigation.

Use the existing authenticated user state when possible.

Do not weaken authentication security.

---

# 9. STORAGE / IMAGE EGRESS

Audit all Supabase Storage images.

Prevent repeated downloads caused by:

* component remounts
* regenerated URLs
* unnecessary image reloads
* loading original images when thumbnails are sufficient
* offscreen images loading unnecessarily

Use lazy loading where appropriate.

For immutable public media, use appropriate browser/CDN caching.

For private media, preserve security.

DO NOT make private files public.

---

# 10. VIDEO — HIGHEST PRIORITY

Video can easily explain a large portion of 1GB/day usage.

For:

* Flicks
* Circle
* Stories

ensure:

### IDLE:

No unnecessary video downloads.

### OFFSCREEN:

Video paused and unnecessary loading prevented.

### BACKGROUND:

Video paused/stopped.

### NAVIGATE AWAY:

Video playback stopped.

### MULTIPLE VIDEOS:

Do not unnecessarily decode multiple videos simultaneously.

Do not preload an entire list of videos.

Prefer loading the active/visible video only.

Preserve the existing one-video Story decoder and Circle viewport gating.

---

# 11. CAMERA / MICROPHONE

Camera and microphone must not remain active outside their required feature.

When:

* camera screen closes
* call ends
* user navigates away
* application backgrounds

stop unnecessary tracks.

Preserve existing Agora cleanup.

---

# 12. AUDIO

Preserve the existing shared AudioContext optimization.

Find remaining AudioContext creation.

Only fix additional contexts if they are genuinely created repeatedly or remain active unnecessarily.

Do not break:

* chat sounds
* notification sounds
* calls
* story music
* UI sounds

---

# 13. TIMERS

Search every:

```text
setInterval
setTimeout
requestAnimationFrame
```

For each determine whether it is:

* one-shot
* intentionally recurring
* incorrectly recurring
* missing cleanup
* running while screen is hidden

Fix confirmed timer leaks.

Any expensive recurring timer should stop when its screen is inactive/backgrounded.

---

# 14. MOBILE BACKGROUND

When:

```text
document.hidden === true
```

or the application enters background:

stop/pause unnecessary:

* video
* audio
* camera
* microphone
* polling
* animation loops
* expensive calculations
* refresh loops

Do not unnecessarily break required authentication behavior.

---

# 15. OBJECT URL MEMORY

Audit every:

```text
URL.createObjectURL()
```

Every temporary upload/preview URL must have an appropriate lifecycle and eventually be revoked.

Do NOT revoke a URL while the media still needs it.

Do not blindly add revoke calls.

---

# 16. REACT EFFECT LEAKS

Look specifically for effects like:

```text
useEffect(() => {
   subscribe()
   setInterval(...)
   addEventListener(...)
}, [...])
```

without matching cleanup.

Every subscription/timer/listener/observer must have a proper cleanup path.

Pay particular attention to effects whose dependency arrays cause repeated creation.

---

# 17. REQUEST DEDUPLICATION

Where the same request can be triggered simultaneously by multiple components, prevent unnecessary duplicate requests.

Example:

```text
BAD:

Home mounted
→ profile request

Header mounted
→ same profile request

Sidebar mounted
→ same profile request

GOOD:

One profile request
→ shared application state
```

Do this only where clearly safe.

---

# 18. DO NOT "FIX" BY BREAKING FEATURES

DO NOT:

* disable Realtime
* remove Chat
* remove videos
* remove Stories
* remove Circle
* remove notifications
* remove authentication
* remove camera
* remove calls
* replace Supabase with fake data
* disable functionality just to reduce bandwidth

The goal is:

**FULL FEATURES + ONLY NECESSARY NETWORK TRAFFIC**

---

# 19. SUPABASE EGRESS PROTECTION

The application should avoid repeatedly downloading identical large data.

For appropriate data:

* reuse existing in-memory state
* paginate large datasets
* avoid unnecessary refetches
* use bounded queries
* select only required columns
* lazy-load expensive media
* prevent duplicate subscriptions

Do not introduce unsafe persistent caching of private user data.

---

# 20. MOBILE CPU/GPU PROTECTION

Audit continuous animations and effects.

Do not remove the new landing page animations automatically.

However, any animation that continuously consumes significant CPU/GPU unnecessarily should be changed to a more efficient CSS transform/opacity animation or paused when not visible.

Use:

* IntersectionObserver
* document visibility
* lifecycle cleanup

where appropriate.

---

# 21. BUILD + REGRESSION

After implementation:

Run:

npm run build

Then:

git diff --check

Check that:

* Login works
* Logout works
* Home works
* Feed works
* Chat works
* Notifications work
* Circle works
* Flicks works
* Stories work
* Profile works
* Uploads work
* Camera works
* Calls work
* Realtime works

---

# 22. IMPORTANT — AUTHENTICATED PROFILING

If an authenticated test session is available, use it.

If it is NOT available:

DO NOT block the implementation on auth-state.json.

Continue with code-level hardening based on confirmed resource leaks.

Do NOT invent runtime measurements.

Clearly distinguish:

CODE-CONFIRMED FIXES

from

RUNTIME-MEASURED RESULTS.

---

# 23. BEFORE/AFTER NUMBERS

Never invent numbers.

If Supabase dashboard data is unavailable, state:

`Exact Supabase egress could not be measured from this environment.`

If physical phone temperature is unavailable:

`Physical device temperature was not measured.`

Do not claim a specific °C reduction.

---

# 24. REQUIRED CHANGE LOG

For EVERY modification provide:

```text
File:
Problem:
Evidence:
Change:
Expected impact:
Risk:
```

Only make changes directly related to:

* Supabase egress
* network traffic
* CPU
* memory
* battery
* thermal load
* lifecycle leaks

Do not make unrelated changes.

---

# 25. FINAL RESULT

The final report must contain:

## ROOT CAUSES FOUND

## SUPABASE / NETWORK FIXES

## REALTIME FIXES

## VIDEO / STORAGE FIXES

## CPU / BATTERY FIXES

## LIFECYCLE / MEMORY FIXES

## FILES CHANGED

## EXISTING PERFORMANCE FIXES PRESERVED

## BUILD / REGRESSION STATUS

## MEASURED RESULTS

Only actual measurements.

## UNMEASURED ITEMS

Clearly state what could not be measured.

## FINAL VERDICT

Choose:

`PERFORMANCE ISSUE RESOLVED`

or

`MAJOR IMPROVEMENT — MORE PROFILING REQUIRED`

or

`SIGNIFICANT RESOURCE LEAK STILL EXISTS`

---

# FINAL INSTRUCTION

This is the actual implementation pass.

Do not send me through another chain of audit-only prompts.

Inspect the existing optimized code, identify confirmed causes of excessive network/CPU/media activity, implement targeted fixes, build the application, and report exactly what was changed.

Preserve the existing features and all previous performance fixes.

The final goal is:

**NO UNNECESSARY SUPABASE EGRESS + NO UNNECESSARY BACKGROUND WORK + LOW MOBILE CPU/HEAT/BATTERY USAGE.**
