FLICKS INDIA — STEP 1 ONLY: SECURITY + SECRETS + RLS

Existing stack: React + Vite + Node + Supabase.

Rollback already exists:
`backup/pre-performance-audit` — `95d2f9f`

IMPORTANT RULES:

* No UI redesign.
* No feature removal.
* No Next.js migration.
* No Neon/R2/Cloudflare migration.
* Do not rewrite architecture.
* Do not disable Supabase Realtime globally.
* Do not blindly replay old SQL migrations.
* Preserve existing behavior.

VERY IMPORTANT — ADMIN EMAIL:
The existing admin email system is intentional and is the PRIMARY admin identification method.

DO NOT:

* remove adminConfig.ts
* remove/change the configured admin email
* replace email authorization with a role/table system
* change existing admin detection logic
* break admin dashboard/moderation

Keep the existing admin-email mechanism exactly as it is.

TASK 1 — SECRET EXPOSURE

Fix confirmed browser-exposed secrets:

1. Gemini:

* `VITE_GEMINI_API_KEY`
* `src/lib/geminiClient.ts`
* `src/lib/suggestionEngine.ts`

Move Gemini calls to a server-side/Edge Function using a server-only environment secret.

Preserve all current Gemini behavior:

* CreatePost suggestions
* captions
* hashtags
* hooks
* recommended suggestion
* fallback behavior

Also remove browser exposure/use of `VITE_GOOGLE_ACCESS_TOKEN` if present.

2. GNews:

* `scripts/autoPost.mjs`

Remove hard-coded API key and use server-only environment secret.

Do not expose secrets through VITE or browser bundles.

Do not print secrets in logs.

TASK 2 — FIX VERIFIED RLS/DATA EXPOSURE

Inspect the CURRENT deployed Supabase policies first.

Do not assume old SQL files equal production.

Fix only these verified areas:

* `user_risk_profiles` → no public read
* `deletion_requests` → no public read
* `game_sessions` → no unrestricted FOR ALL
* `admin_earnings` → prevent arbitrary insert/update
* Circle posts/comments/likes/views → respect membership/ownership
* Circle views → viewer must match `auth.uid()`
* `votes` → do not expose `user_id` publicly
* profiles → protect mobile, pincode, private location, suspension/internal fields
* public posts → preserve public posts but protect private/non-public posts
* AutoPost → prevent arbitrary client creation of `is_admin_post=true`

Preserve all legitimate functionality.

Create a focused SQL patch/migration if required.

DO NOT replay master/full-backup/step migrations blindly.

TASK 3 — VALIDATION

After changes:

* run production build
* verify login/logout
* verify existing admin email detection
* verify admin dashboard
* verify Gemini feature
* verify AutoPost
* verify public/private post behavior

Check that Gemini/GNews/Google access tokens are NOT present in the browser bundle.

Do not rotate real leaked keys yourself unless the environment already provides the mechanism. Report that rotation is required separately.

FINAL RESPONSE:
Tell me:

1. Files changed
2. SQL patch created
3. Security issues fixed
4. Admin-email system confirmed untouched
5. Build/test result
6. Anything unsafe to change because production state could not be verified

STOP AFTER STEP 1. Do not start performance/media work.
