## ADD PROFANITY FILTER WORD VARIANTS — STRICTLY ISOLATED CHANGE

The app already has an existing profanity/abusive-word filtering system.

### CRITICAL: THIS MUST BE A TINY, ISOLATED CHANGE ONLY.

DO NOT create a new profanity system.

DO NOT redesign or replace the existing filter.

DO NOT refactor the existing filtering architecture.

DO NOT modify the masking/hiding UI.

DO NOT modify unrelated code.

The ONLY requested change is to extend the EXISTING profanity dictionary/normalization logic with these three variants:

* `bhosdiwala`
* `bhosdiwale`
* `bhosdiwali`

They must be detected and hidden exactly like the profanity words that are already filtered.

Case variations must also work:

* `Bhosdiwala`
* `BHOSDIWALE`
* `bHosDiWaLi`

Use the existing normalization logic.

---

# ABSOLUTE PROTECTION — DO NOT TOUCH THESE SYSTEMS

This profanity change must NOT modify, refactor, remove, or interfere with ANY of the following:

### Performance / Supabase

* Supabase queries
* Supabase Realtime
* Supabase Storage
* Supabase caching
* Supabase egress/network optimization
* request deduplication
* query limits
* pagination
* realtime channel lifecycle
* realtime subscriptions
* authentication/session logic

### Mobile Performance

* CPU optimization
* battery optimization
* mobile heating fixes
* background/foreground lifecycle
* timers
* polling
* video lifecycle
* audio lifecycle
* camera/microphone lifecycle
* object URL cleanup
* Web Audio optimization
* memory cleanup

### Existing Features

* Home/Fame Feed
* Chat
* Notifications
* Circle
* Flicks/Reels
* Stories
* Profile
* Posts
* Comments
* Likes
* Uploads
* Camera
* Agora calls
* Authentication
* Landing page
* Routing

ALL previously implemented performance fixes must remain exactly as they are.

---

# DO NOT ACCIDENTALLY INTRODUCE NETWORK/CPU WORK

The profanity filter is expected to remain a local/client-side operation wherever the existing system currently performs it.

DO NOT:

* add a Supabase query
* add an API request
* add polling
* add a Realtime subscription
* add a timer
* add an external profanity API
* add a new background process
* add unnecessary React effects
* add continuous processing

Adding these would be an unacceptable regression.

The new words must simply be added to the existing profanity data/logic.

---

# DO NOT CHANGE EXISTING BEHAVIOR

Existing profanity words must continue working exactly as before.

Do NOT:

* remove existing words
* change existing word matching behavior
* change masking characters
* change UI
* change error messages
* change moderation behavior
* change database behavior
* change post/chat/comment submission flow

Only extend the existing word list/normalization data.

---

# TEST

Test:

1. `bhosdiwala`
2. `bhosdiwale`
3. `bhosdiwali`

Also test:

* `Bhosdiwala`
* `BHOSDIWALE`
* `bHosDiWaLi`

Confirm each is handled exactly like an already-blocked abusive word.

Confirm existing profanity filtering still works.

---

# CHANGE SCOPE CHECK

Before finishing, verify:

* Only the existing profanity dictionary/filter source was changed.
* No Supabase files were changed.
* No Realtime files were changed.
* No performance-related files were changed.
* No video/audio/camera files were changed.
* No authentication files were changed.
* No routing files were changed.
* No unrelated refactoring occurred.

If the profanity dictionary is located inside a larger file, make the SMALLEST possible edit.

Do not reformat the entire file.

Do not reorder unrelated code.

Do not perform cleanup/refactoring.

---

# VERIFICATION

Run:

`npm run build`

Then:

`git diff --check`

Finally inspect the git diff.

If the diff contains anything unrelated to adding these profanity variants, revert the unrelated changes before finishing.

### FINAL RESPONSE MUST INCLUDE:

* Exact file changed
* Exact words added
* Confirmation that existing profanity behavior is unchanged
* Confirmation that Supabase/Realtime/performance/mobile-heating code was NOT modified
* Build result
* `git diff --check` result

### FINAL RULE

This is a **single-purpose dictionary update**.

Do not use this task as an opportunity to optimize, refactor, redesign, clean up, or modify anything else.

**ONE CHANGE ONLY: add the three profanity variants to the existing filter.**
